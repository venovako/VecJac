#ifndef SJROT_H
#define SJROT_H

#include "vec.h"

extern float sjrot_(const fint n[static restrict 1], float x[static restrict VSL], float y[static restrict VSL], const float t[static restrict 1], const float c[static restrict 1]);

#endif /* !SJROT_H */
