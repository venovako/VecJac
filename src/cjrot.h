#ifndef CJROT_H
#define CJROT_H

#include "vec.h"

extern float cjrot_(const fint n[static restrict 1], float xr[static restrict VSL], float xi[static restrict VSL], float yr[static restrict VSL], float yi[static restrict VSL], const float t[static restrict 1], const float c[static restrict 1], const float ca[static restrict 1], const float sa[static restrict 1]);

#endif /* !CJROT_H */
