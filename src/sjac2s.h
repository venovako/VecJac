#ifndef SJAC2S_H
#define SJAC2S_H

#include "vec.h"

extern fint sjac2s_(const fnat n[static restrict 1], const float a11[static restrict VSL], const float a22[static restrict VSL], const float a21[static restrict VSL], float cs[static restrict VSL], float sn[static restrict VSL], float l1[static restrict VSL], float l2[static restrict VSL], unsigned p[static restrict 1]);

#endif /* !SJAC2S_H */
